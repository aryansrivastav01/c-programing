#include<stdio.h>
int  main(){
    // defining variable a 
    int a;
    
    printf("the value of first variable is:");
    scanf("%d",&a);
    printf("the entered first value is:%d",a);
    return 0;
}