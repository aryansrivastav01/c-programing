#include<stdio.h>
int  main(){
    // defining variable a 
    int a;
    int b;
    int c;
    printf("the value of first variable is: ");
    scanf("%d",&a);
     printf("the value of second variable is: ");
     scanf("%d",&b);
      printf("the value of third variable is: ");
      scanf("%d",&c);
     printf("the entered first value is:%d \t the entered second value is %d\t  the entered third value is %d",a,b,c);
    return 0;
}